# Live pre-push state: contains the real GHL token + live client config.
# Regenerated by snapshot.py immediately before each push. Never commit.
_snapshots/

# Per-workstream local artifacts (fresh per run, may contain live config or
# proposed mutations with secrets re-injected at push time). Never commit.
_artifacts/
_reports/
